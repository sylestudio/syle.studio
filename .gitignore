/target
**/*.rs.bk
*.pdb
.DS_Store

# Leptos / trunk
/admin/dist
/admin/node_modules
/admin/styles/tailwind.css

# Astro
/web/node_modules
/web/dist
/web/.astro
# Reproducible responsive derivatives are generated by the Rust pipeline before
# every Astro build. The small source-to-variant manifest remains versioned.
/web/public/project-media

# env / secrets
.env
.env.*
!.env.example

# media store (originals + derivatives live on the VPS, not in git)
/media

# lanzador de dev local del API (rutas propias de cada maquina)
/run-api.ps1
