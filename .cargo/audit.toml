[advisories]
# `rsa` is locked only through SQLx's optional MySQL driver. This workspace
# disables SQLx default features and enables PostgreSQL exclusively, so neither
# the crate nor its vulnerable private-key operation enters any build target.
# RustSec has no patched `rsa` release for this advisory yet.
ignore = ["RUSTSEC-2023-0071"]
